# Зависимости интерфейса поверх самого FEDOT.MAS.
# FEDOT.MAS ставится отдельно (uv sync в корне репозитория) — здесь только то,
# что нужно веб-стенду и чего нет в зависимостях библиотеки.
fastapi>=0.115
uvicorn>=0.30
python-dotenv>=1.0
openai>=1.40
# Поиск по реестру MCP (/api/mcp_search). Сейчас httpx приезжает транзитивно
# через huggingface_hub, но полагаться на чужую цепочку зависимостей нельзя.
httpx>=0.27
# Приём файлов-источников (/api/upload): FastAPI разбирает multipart через него
python-multipart>=0.0.9
