"""Текстовые шаблоны, которые уходят моделям.

Вынесены отдельно, потому что это не логика, а контент: их правят чаще всего,
и читать их удобнее подряд, а не вперемешку с обработчиками запросов.
"""

from __future__ import annotations

RETRY_HINT = """

ВАЖНО: предыдущая попытка сборки конфигурации не прошла проверку со следующей ошибкой:
{error}
Собери конфигурацию заново так, чтобы этой ошибки не было. Каждый агент, упомянутый
в пайплайне, обязан присутствовать в пуле агентов ровно с тем же именем; лишних имён
в пайплайне быть не должно."""


MAW_TEAM_SIZE_PROMPT = """

Требование к размеру команды: создай ровно пять содержательных агентов. Рекомендуемый
состав — три предметные роли, отдельный агент-сборщик и отдельный агент-проверяющий.
Каждая роль должна иметь самостоятельную ответственность и уникальный результат;
не добавляй фиктивных агентов, которые только повторяют чужую работу. Предметные роли,
не зависящие друг от друга, располагай параллельно. Агент-сборщик должен объединить все
их результаты в полный ответ, после чего агент-проверяющий проверяет итог. Если по смыслу
задачи один из трёх предметных агентов должен выполняться раньше остальных, отрази эту
зависимость в графе, сохранив общее число агентов равным пяти.
"""


PREPARE_PROMPT = """Пользователь описал задачу для мультиагентной системы одним текстом.
Раздели его на две части, которые нужны FEDOT.MAS:

1. task — постановка для мета-агента: что должна уметь система, какие роли агентов нужны,
   что выполняется параллельно, где нужны инструменты ({tools_hint}), кто проверяет результат.
   Если пользователь не описал архитектуру, придумай её сам исходя из задачи.
   Обязательно добавь требование: имена агентов и инструкции — на русском языке.
   Для MAW обязательно включи в task требование создать ровно пять содержательных агентов:
   три предметные роли, отдельного агента-сборщика и отдельного агента-проверяющего. Роли
   не должны дублировать друг друга только ради количества.
   Агентов дели ПО ПРЕДМЕТНЫМ ОБЛАСТЯМ задачи (например: правовые ограничения, транспорт и
   инфраструктура, расчёт показателей), а не по инструментам. Один агент отвечает за свою тему
   целиком и получает все инструменты, которые ему для этого нужны. Роли вида «агент веб-поиска»,
   «агент песочницы», «агент документов» запрещены — это инструменты, а не предметные области.
   ОБЯЗАТЕЛЬНО предусмотри отдельного агента-сборщика: он идёт предпоследним, получает
   артефакты всех предметных агентов и составляет из них ЕДИНЫЙ полный ответ пользователю.
   Без него ответом окажется артефакт одного агента — кусок работы вместо результата.
   Порядок всегда такой: предметные агенты (по возможности параллельно) -> агент-сборщик
   -> агент-проверяющий. Проверяющий только оценивает работу сборщика и НЕ является автором
   ответа. Цикл (loop) допускается только вокруг одной узкой доработки; оборачивать в цикл
   весь пайплайн запрещено — агенты будут переваривать один и тот же контекст по кругу.
   Агенту, который выполняет расчёты, назначь песочницу (sandbox или sandbox-light) и потребуй
   ВЫЧИСЛЯТЬ в ней, получая конкретные числа. Инструмент sequential-thinking ему НЕ назначай:
   с ним агент начинает рассуждать вместо счёта и возвращает описания вместо показателей.
   Агент-сборщик обязан обеспечить согласованность итога: выводы, ранжирования и таблицы
   не должны противоречить друг другу. Если в тексте сказано, что вариант X предпочтительнее
   Y, то и в итоговой таблице X обязан стоять выше; если названа величина, она обязана
   совпадать с таблицей. Перед выдачей пройди по всем выводам и сверь каждый с таблицами —
   расхождение между таблицей и текстом обесценивает верные расчёты и проигрывает сравнение.
   Агент-сборщик обязан перенести в итог ВСЕ конкретные числа, таблицы и ссылки, полученные
   предметными агентами, дословно, и начать итог с блока исходных данных, где перечислены все
   величины, использованные в расчётах, с их статусом. Числа, появляющиеся в выводах, но
   отсутствующие в этом блоке, — скрытые допущения, их быть не должно. Заменять их общими описаниями («определяется по ПЗЗ»,
   «зависит от зоны») запрещено — это главная причина плохого ответа.
   Если предусматриваешь цикл доработки, ограничь его 2 итерациями, а в инструкции сборщика
   укажи: при доработке он берёт свой предыдущий полный ответ и ТОЛЬКО дополняет и исправляет
   его по замечаниям; сокращать и обобщать ранее приведённые числа нельзя.
   Если задача требует фактических данных (ряды наблюдений, погода, измерения), а ссылки
   на источник в тексте пользователя НЕТ — подставь её сам в поле query из справочника ниже,
   прямо указав адрес выгрузки. Измерено на прогонах: когда ссылка названа в задаче, агент
   скачивает файл в 6 случаях из 6; когда не названа — в 1 из 8, остальные подменяют факты
   справочными значениями. Справочник: суточная погода за прошедший период (температура,
   ветер, солнечная радиация) — архив Open-Meteo вида
   https://archive-api.open-meteo.com/v1/archive?latitude=ШИР&longitude=ДОЛГ&start_date=ГГГГ-ММ-ДД&end_date=ГГГГ-ММ-ДД&daily=ПАРАМЕТР&timezone=Europe%2FMoscow&format=csv
   где ПАРАМЕТР это temperature_2m_mean, wind_speed_10m_max или shortwave_radiation_sum;
   координаты подставь по названному в задаче городу.
   Если в тексте пользователя есть ссылка на файл с данными, предусмотри агента, который
   СКАЧИВАЕТ этот файл (download), читает его (document) и считает по нему в песочнице
   sandbox-light, перенося значения из таблицы в код списком. Веб-поиск такой файл не заменяет,
   и справочные значения вместо данных из файла подставлять запрещено.
   Если задача аналитическая и данных в ней нет (выбор решения, сравнение подходов),
   потребуй строить вывод на явных критериях: таблица критериев с оценками по каждому варианту,
   каждый фактический довод — с числом и ссылкой; вывод обязан следовать из таблицы.
   Обязательно включи в постановку эти правила работы агентов:
   - агент поиска обязан сделать НЕСКОЛЬКО запросов по разным аспектам задачи (минимум четыре:
     по объекту, по применимым правилам и нормам, по территории, по расчётным нормативам)
     и вернуть найденное СПИСКОМ: утверждение — ссылка. Сокращать список нельзя;
   - искать не только по точному идентификатору из запроса (номера, артикулы, коды часто не
     индексируются): если по нему ничего не найдено, искать по более общим признакам —
     территории, категории, отрасли, применимым нормам и регламентам;
   - последний агент обязан выдать полный ответ по существу: таблицы, числа, расчёты и выводы;
     формулировки вида «оценка невозможна без дополнительных данных» и списки того, что нужно
     дополнительно запросить ВМЕСТО ответа — запрещены; недостающие величины берутся как типовые
     по применимым нормам, расчёт всё равно выполняется и приводится;
   - финальный агент обязан перенести ссылки из результатов поиска в итоговый ответ и привязать
     их к конкретным пунктам; ответ без ссылок считается неполным;
   - числа приводятся конкретными значениями, а не широкими диапазонами: если норма задаёт вилку,
     выбирается расчётное значение и объясняется выбор;
   - итоговая таблица с рассчитанными показателями обязательна;
   - ответ привязывается к конкретному контексту задачи (регион, отрасль, применимые своды правил
     и местные регламенты с их номерами), общие формулировки без привязки не принимаются;
   - нормативное значение (температура по СП, коэффициент, предельный параметр) приводится
     только с цитатой из найденного документа; если документ не открыт — величина помечается
     «требует подтверждения» и не выдаётся за установленную;
   - в ответе каждый факт помечается статусом: установлено по источнику (со ссылкой),
     принято как типовое значение для расчёта, требует официального подтверждения;
   - агент-проверяющий следит за соблюдением этих правил и возвращает работу на доработку,
     если ответ неполон или факт остался без статуса.
2. query — то, что система будет решать: сам вопрос и исходные данные из текста пользователя,
   без описания архитектуры. Данные из текста перенеси в query дословно, ничего не выдумывая.
   Если данных в тексте нет, оставь только сам вопрос. В конец query добавь абзац:
   «Если точных данных найти не удалось, не отказывайся от оценки и не подменяй её перечнем
   необходимых запросов: возьми типовые значения по применимым нормам, доведи расчёт до чисел
   и приведи итоговые показатели в таблице. Каждый факт помечай: установлено по источнику
   (со ссылкой), принято типовым для расчёта, требует официального подтверждения.»

Тип системы: {kind} ({kind_hint}).

ТЕКСТ ПОЛЬЗОВАТЕЛЯ:
{text}

Верни строго JSON: {{"task": "...", "query": "..."}}"""


TOOL_DESCRIPTIONS = {
    "websearch-searxng": "websearch-searxng — веб-поиск через локальный SearXNG",
    "sandbox-light": "sandbox-light — расчёты на чистом Python (только builtins). ВАЖНО: файлы ему недоступны, поэтому данные, прочитанные инструментом document, передаются в код значениями (списком прямо в тексте кода), а не путём к файлу",
    "sandbox": "sandbox — полная песочница Python: pip, файлы, длинные вычисления",
    "sequential-thinking": "sequential-thinking — пошаговый разбор сложных рассуждений",
    "document": "document — чтение PDF, DOCX, XLSX, CSV и архивов по пути к файлу",
    "download": "download — скачать файл по ссылке (в паре с document для выписок и регламентов)",
    "media": "media — разбор изображений, аудио и видео",
    "web-scraping": "web-scraping — открыть страницу и вытащить её содержимое, когда сниппетов поиска мало",
    "browser-usage": "browser-usage — интерактивный браузер: клики, формы, скриншоты",
    "youtube-transcript": "youtube-transcript — расшифровки видео с YouTube",
    "rubber-recipe-predictor": "rubber-recipe-predictor — предсказание четырёх свойств заданного рецепта SBR/NR/N220 по открытым данным без изменения состава",
    "technology-card-audit": "technology-card-audit — извлечение норм из демонстрационной ТТК и программная проверка отклонений фактической выработки",
}


DATA_SOURCE_HINT = """

Источник фактических данных (открытый архив Open-Meteo, ключ не нужен, отдаёт CSV):
https://archive-api.open-meteo.com/v1/archive?latitude=ШИРОТА&longitude=ДОЛГОТА&start_date=ГГГГ-ММ-ДД&end_date=ГГГГ-ММ-ДД&daily=ПАРАМЕТР&timezone=Europe%2FMoscow&format=csv
Подставь координаты названного в задаче города и нужный период, а вместо ПАРАМЕТР —
temperature_2m_mean для суточной температуры, wind_speed_10m_max для ветра,
shortwave_radiation_sum для солнечной радиации (можно перечислить через запятую).
Скачай файл этим адресом и считай по нему: справочные и усреднённые значения из статей
фактическими данными не являются."""


JUDGE_PROMPT = """Ты — придирчивый судья-верификатор. Твоя работа — не оценить изложение,
а проверить числа и внутреннюю согласованность. Красивый связный текст без проверяемых величин
преимуществом НЕ является.

Запрос и оба ответа придут отдельным сообщением.

ШАГ ПЕРВЫЙ, до чтения правил: если в ответах есть числа, СРАЗУ вызови sandbox-light
и пересчитай ключевые из них. Вердикт без единого вызова песочницы при споре о числах
не принимается и будет отправлен на повтор — это лишние минуты, поэтому считай сразу.

У тебя есть песочница sandbox-light. ВСЕ пересчёты — суммы рядов, средние, доли, переводы единиц,
дисконтирование — выполняй ТОЛЬКО вызовом песочницы: пересчёт, сделанный в уме, не считается
выполненным и основанием для вердикта быть не может. На длинных рядах устный счёт ошибается,
и ты объявишь ошибкой верный чужой расчёт — это уже случалось. Прежде чем объявить чужое число
неверным, пересчитай его в песочнице ДВАЖДЫ: расхождение существует, только если оба твоих
пересчёта совпали между собой и разошлись с ответом. Если величину не удалось пересчитать
в песочнице, пометь её «не проверено».

ПОРЯДОК РАБОТЫ — выполни до того, как назовёшь победителя:
1. Пересчитай ключевые величины по тем же формулам и исходным данным, которые ответ приводит сам.
   Если его собственный итог не сходится с пересчётом — это ошибка ответа, даже небольшая.
2. Проверь единицы и коэффициенты перевода (кВт и Гкал, м³ и ккал, рубли с НДС и без).
2а. Сравнения с порогами («меньше 8,6», «ниже 1380», «в диапазоне 200–240») проверяй
   вычислением в песочнице, а не на глаз. Ошибка в сравнении меняет вывод так же, как ошибка
   в сумме, и ты уже ошибался здесь: заявлял превышение порога там, где значение было ниже.
   Потерянный КПД, множитель или перепад температур — грубая ошибка, а не мелочь.
3. Проверь, что вывод следует из приведённых чисел. Пример противоречия: если у одного варианта
   и капитальные, и эксплуатационные затраты не выше, точки пересечения затрат быть не может.
4. Проверь правдоподобие производных величин (часы использования максимума нагрузки, удельные
   расходы, сроки окупаемости). Величина, выходящая за физически осмысленный диапазон, — ошибка,
   даже если подана уверенно и со ссылкой.
5. Числа, приведённые диапазоном там, где запрос требовал конкретного значения, засчитывай как
   невыполненное требование.
5а. ВЗВЕШИВАЙ ОШИБКИ ПО СУЩЕСТВЕННОСТИ, одинаково для обеих сторон. Решающими считаются
   ошибки, которые меняют вывод, нарушают тождество, теряют множитель или единицу, либо
   отличаются от верного значения больше чем на 1 процент относительной величины.
   Расхождение в последнем знаке, не меняющее вывод и укладывающееся в 1 процент
   (например, −21,78 против −21,83), отмечай в проверках как неточность, но решающим
   доводом не делай и победу из-за него не присуждай. Придирка к округлению — такая же
   ошибка судьи, как пропуск грубого просчёта.
6. ПРОИСХОЖДЕНИЕ ДАННЫХ. Этот пункт применяется, ТОЛЬКО если запрос прямо требовал взять данные
   из источника (файл по ссылке, реестр, документ, действующий тариф) или выдать фактические,
   измеренные значения. Если запрос таких данных не требовал, напиши «не применимо»: округлые
   проектные допущения вроде «капзатраты 20 млн» — это нормальные исходные условия расчёта,
   а не вымысел, и придираться к отсутствию у них ссылок нельзя.
   Там, где пункт применяется, проверь по каждой ключевой величине, сказано ли, откуда она
   взялась: путь к скачанному файлу, ссылка, название и дата документа. Величина, поданная
   как фактическая или измеренная, но без указания происхождения, считается неподтверждённой.
   Признаки выдуманных данных, каждого из них достаточно самого по себе:
   — оговорки «условный», «для примера», «предположим», «допустим», «сгенерирован», «примерный
     ряд», «в отсутствие данных возьмём»;
   — ряд наблюдений без указания, откуда он получен, особенно если ответ нигде не упоминает
     ни скачивания, ни чтения файла;
   — подозрительная гладкость ряда: малый разброс, одинаковый шаг между значениями, одинаковое
     округление, отсутствие выбросов и экстремумов, неизбежных в реальных наблюдениях.
     ВАЖНО в обратную сторону: зубчатость, резкие перепады и экстремумы — нормальное свойство
     настоящих наблюдений, а не признак «ручного набора»; подозрительно их отсутствие,
     а не наличие;
   — величины, противоречащие другим утверждениям того же ответа.
   Отсутствие этих признаков подлинности не доказывает, но их наличие означает, что расчёт
   выполнен поверх вымысла и его аккуратность значения не имеет.
   ВАЖНО не перегнуть: длинный ряд наблюдений (сотни значений) печатать целиком неразумно.
   Если ответ приводит точный адрес выгрузки, число полученных строк, крайние значения с датами
   и таблицу агрегатов, которые между собой сходятся, — происхождение считается подтверждённым,
   даже когда сам ряд не приведён построчно. Вымыслом это называть нельзя.

ЧЕГО ДЕЛАТЬ НЕЛЬЗЯ:
— опираться на собственные воспоминания о внешних фактах, в которых ты не уверен (значения тарифов,
  коды регионов, номера и редакции нормативов). Такой факт помечай «не проверено» и НЕ делай его
  доводом в пользу победителя. Назначить победителя из-за факта, в котором ты не уверен, — грубая
  ошибка судьи;
— утверждать, каким «на самом деле» является значение из источника, которого у тебя нет: содержимое
  скачанного файла, ряды наблюдений, показания приборов. Ты их не видел. Проверяй только внутреннюю
  согласованность приведённых чисел (сходятся ли они между собой), а не их соответствие источнику;
— засчитывать полноту разделов вместо правильности чисел. Ошибка в расчёте весит больше, чем
  отсутствующий раздел. Внутреннее противоречие — тяжелейший дефект;
— объявлять чужой расчёт ошибочным, если ты не воспроизвёл его сам. Не сошлось с первой попытки —
  пересчитай внимательнее; если воспроизвести не удалось, пометь «не проверено», но НЕ записывай
  это в ошибки;
— присуждать победу или ничью ответу, в котором нет требуемых расчётов и чисел, если запрос их
  требовал. Отсутствие расчёта — это невыполненное задание, а не осторожность. Ответ без чисел
  проигрывает ответу с числами даже при неточности в последних, и ничья здесь тоже неуместна:
  выполненная работа против невыполненной — это победа, а не равенство. Исключение одно —
  числа выданы за фактические, но взяты из вымысла;
— считать достоинством уверенный тон и обилие ссылок сами по себе;
— объявлять дату документа «из будущего» или «невозможной», сверяясь со своим представлением
  о сегодняшнем дне: какой сегодня день, тебе неизвестно. Дату можно ставить под сомнение
  только при противоречии внутри самого ответа (документ младше периода, который он вводит).

ЕСЛИ ОБА ОТВЕТА ЧИСЛЕННО ВЕРНЫ и приходят к одинаковым выводам — это НИЧЬЯ. Полнота
изложения, показ промежуточных шагов, оформление таблиц, детализация и структура не являются
основанием для победы: это форма подачи, а не правильность. Присуждать победу за то, что один
ответ «нагляднее» или «подробнее», при совпадающих числах и выводах — ошибка судьи.

Ответ, который признаёт, что исходные данные выдуманы, сгенерированы или взяты «для примера»
вместо фактических, считается несостоятельным и победить не может — сколько бы аккуратной ни была
арифметика поверх вымысла. Честный отказ с объяснением, каких данных не хватило, лучше вымышленных
чисел: при выборе между вымыслом и отказом побеждает отказ.

Если оба ответа содержат сопоставимые по тяжести ошибки в расчётах — это ничья, даже когда один
оформлен заметно лучше.

Ответь строго в этом формате и в этом порядке:
ПРОИСХОЖДЕНИЕ ДАННЫХ: по строке на каждый ответ — откуда взяты ключевые величины и есть ли
признаки вымысла. Если запрос не требовал внешних данных, напиши «не применимо».
ПРОВЕРКИ: 3–6 строк, каждая — что проверял, что получилось при пересчёте, у кого ошибка.
ПОБЕДИТЕЛЬ: A | Б | ничья
ПОЧЕМУ: 2–4 предложения по-русски, опираясь только на выполненные проверки.
ЧЕГО НЕ ХВАТАЕТ В ПРОИГРАВШЕМ: одно предложение."""


EFFORT_PROMPT = """Оцени, сколько заняла бы РУЧНАЯ разработка такой мультиагентной системы
у одного инженера — без FEDOT.MAS, вручную: разбор постановки, проектирование ролей агентов,
подбор и подключение инструментов, написание промптов, отладка взаимодействия, прогоны.
Считаем разработку системы, а не выполнение самой задачи предметным специалистом.

ПОСТАНОВКА:
{task}

СОСТАВ СИСТЕМЫ, КОТОРУЮ СОБРАЛ МЕТА-АГЕНТ:
{config}

Опирайся на то, что реально влияет на трудоёмкость: число агентов и связей между ними,
число подключаемых инструментов, наличие параллельных веток и циклов доработки,
объём отладки промптов. Типовые ориентиры: простой линейный пайплайн из 2–3 агентов
без инструментов — около одного человеко-дня; 4–6 агентов с веб-поиском, песочницей
и проверяющим — несколько человеко-дней.

Верни строго JSON:
{{"estimate": "≈ N чел.-дн.", "detail": "1–2 предложения: из чего сложилась оценка"}}
Значение estimate — короткая строка для таблицы, не длиннее 20 символов."""


JUDGE_CONTENT = """ЗАПРОС:
{query}

ОТВЕТ А (мультиагентная система FEDOT.MAS):
{a}

ОТВЕТ Б (одна модель без системы):
{b}"""


BREAKDOWN_PROMPT = """Разложи задачу на подзадачи так, как их выполнял бы инженер вручную,
без мультиагентной системы, и оцени каждую по времени.

ЗАДАЧА:
{task}

СОСТАВ СИСТЕМЫ, КОТОРУЮ СОБРАЛ МЕТА-АГЕНТ (ориентир по объёму работ):
{config}

Требования к разбору:
— от 4 до 8 подзадач, каждая — законченный шаг работы, а не абстракция;
— формулируй действием: «собрать исходные данные по объекту», «свести таблицу и посчитать
  показатели», «найти и проверить тариф в официальном документе», «оформить итоговый отчёт»;
— оценка времени в часах, дробная допускается (0.5, 1.5, 3);
— оценивай реальный ручной труд специалиста, а не работу модели;
— отдельной подзадачей учитывай проверку и перепроверку результата, если она нужна.

Верни строго JSON:
{{"subtasks": [{{"name": "...", "hours": 2.5, "note": "что именно делается"}}, ...]}}"""
