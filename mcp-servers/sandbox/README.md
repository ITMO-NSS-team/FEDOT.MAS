# mcp-sandbox

Full Python sandbox via [E2B](https://e2b.dev), e.g. pip install, file I/O, shell commands, persistent Jupyter-style sessions.

## Prerequisites

- `E2B_API_KEY` environment variable (get one at https://e2b.dev)
