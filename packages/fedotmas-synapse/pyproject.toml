[build-system]
requires = ["uv_build>=0.10.3,<0.11.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "fedotmas_synapse"

[project]
name = "fedotmas-synapse"
version = "0.1.0"
description = "Synapse Platform integration for FEDOT.MAS"
requires-python = ">=3.12"
dependencies = [
    "fedot-mas",
]
